export function dogFactory(name, breed) {
  return { name, breed };
}

export function catFactory(name, breed) {
  return { name, breed };
}

// export { dogFactory, catFactory }; // only use this method when exporting multiple funtions
